import React from 'react';

export interface ChanukkiahProps {
  // Add any props if you expose them later, currently it takes none or standard div props?
  // Looking at code: it takes no props directly, just internal state.
  // But maybe style or className?
}

export declare const Chanukkiah: React.FC<ChanukkiahProps>;
